require 'spec_helper'

describe Profit do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Profit.create!(@valid_attributes)
  end
end
