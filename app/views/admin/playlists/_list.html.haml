%table#playlists
  %tr
    %th Название
    %th Описание
  - @playlists.each_with_index do |playlist, index|
    = render :partial => "admin/playlists/playlist", :locals => {:playlist => playlist, :index => index}
= paginate @playlists
