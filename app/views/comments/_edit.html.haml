= semantic_form_for @comment,
                    :url => comment_path(commentable.class.model_name.downcase, commentable, @comment),
                    :html => { :class => "add-comment" } do |f|
  = render :partial => 'form', :locals => {:f => f}
